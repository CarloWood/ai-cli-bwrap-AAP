The existing no-argument aap-ls behavior remains compatible: it still validates/fixes plan invariants, lists the current objective's parent and siblings, and prints the current objective description using the existing heading.
